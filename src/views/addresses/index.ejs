<div class="d-flex flex-wrap align-items-start justify-content-center h-100 w-100">
    <div class="my-3">
        <h1><u>Address</u></h1>

        <div class="w-100">
            <% if (address) { %>
                <form class="form-horizontal" method="post" action=<%- '/address/' + address.id + '?_method=PUT' %>>
            <%} else { %>
                <form class="form-horizontal" method="post" action="/address">
            <% } %>
                <div class="form-group">
                    <label for="content" class="cols-sm-2 control-label">Endereço</label>
                    <div class="cols-sm-10">
                        <div class="input-group">
                            <span class="input-group-addon"></span>
                            <input type="text" class="form-control" name="content" id="content"
                                placeholder="Insira o endereço completo"
                                <% if (address) { %>
                                    <%- 'value="' + address.content + '"' %>
                                <% } %>
                                />
                        </div>
                    </div>
                </div>

                <div class="form-group">
                    <label for="zipCode" class="cols-sm-2 control-label">Código Postal (CEP)</label>
                    <div class="cols-sm-10">
                        <div class="input-group">
                            <span class="input-group-addon"></span>
                            <input type="text" class="form-control" name="zipCode" id="zipCode"
                                placeholder="Insira o CEP"
                                <% if (address) { %>
                                    <%- 'value="' + address.zipCode + '"' %>
                                <% } %>
                                />
                        </div>
                    </div>
                </div>
    
                <div class="form-group ">
                    <button type="submit" class="btn btn-primary btn-lg btn-block login-button">Salvar</button>
                </div>
            </form>
        </div>


    </div>
</div>